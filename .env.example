# Copy this file to .env and fill in ONE of these (never both need to be real).
# .env is already in .gitignore - it will never be committed.

ANTHROPIC_API_KEY=sk-ant-your-key-here
# OPENAI_API_KEY=sk-your-key-here
# LLM_PROVIDER=anthropic   # only needed if you set BOTH keys above and want to force one
